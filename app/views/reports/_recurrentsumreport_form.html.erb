<%= f.input :chart_type, :as => :select, :collection => [:line, :bar], :include_blank => false %>
<%= f.input :recurrence, :as => :select, :collection => [:daily, :weekly, :monthly, :yearly], :include_blank => false %>
<%= f.input :tag_groups %>