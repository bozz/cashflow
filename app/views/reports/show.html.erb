<% title "Report" %>

<p>
  <strong>Name:</strong>
  <%=h @report.name %>
	<%= render :partial => @report.type.downcase, :locals=>{:report=>@report} %>
</p>

<p>
  <%= link_to "Edit", edit_report_path(@report) %> |
  <%= link_to "Destroy", @report, :confirm => 'Are you sure?', :method => :delete %> |
  <%= link_to "View All", reports_path %>
</p>
